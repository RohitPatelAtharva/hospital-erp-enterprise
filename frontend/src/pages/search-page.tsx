import { Search } from 'lucide-react';
import { PlaceholderPage } from '@/components/feedback/placeholder-page';

export function SearchPage() {
  return (
    <PlaceholderPage
      title="Search"
      description="The Search registry screen (08-UI §)."
      icon={Search}
    />
  );
}
