import { GitBranch } from 'lucide-react';
import { PlaceholderPage } from '@/components/feedback/placeholder-page';

export function VersionsPage() {
  return (
    <PlaceholderPage
      title="Versions"
      description="The Versions registry screen (08-UI §)."
      icon={GitBranch}
    />
  );
}
